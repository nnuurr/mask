using System;
using UnityEngine;

public class MaskVision : MonoBehaviour
{
    [SerializeField]
    bool[] Masks = new bool[5];
    

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
